import 'package:flutter/material.dart';

class TabBarThree extends StatelessWidget {
  const TabBarThree({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('TabBarThree'),
    );
  }
}