import 'package:flutter/material.dart';

class TabBarTwo extends StatelessWidget {
  const TabBarTwo({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('TabBarTwo'),
    );
  }
}